#!/bin/sh
set -e
if ! id -u monitor-agent >/dev/null 2>&1; then
  useradd --system --no-create-home --shell /usr/sbin/nologin monitor-agent
fi
chown -R monitor-agent:monitor-agent /etc/monitor-agent 2>/dev/null || true
chmod 640 /etc/monitor-agent/config.toml 2>/dev/null || true
#DEBHELPER#
exit 0
