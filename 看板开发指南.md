# 看板 / 接收端开发指南

本指南面向 **接收端（看板/统计/告警系统）的开发者**，说明 monitor-agent 上报数据的完整字段、HTTP 约定、时序与幂等处理，便于对接入库与展示。

> monitor-agent 只负责「采集 + 推送」：周期性向配置的 `server_url` POST 一条 JSON。存储、分析、告警、可视化全部在接收端实现。

## 目录

- [上报协议](#上报协议)
- [完整 JSON 示例](#完整-json-示例)
- [字段说明](#字段说明)
- [主机标识与去重](#主机标识与去重)
- [时间戳与时序处理](#时间戳与时序处理)
- [数据语义](#数据语义)
- [HTTP 响应约定与重试](#http-响应约定与重试)
- [幂等与去重](#幂等与去重)
- [缺省与容错](#缺省与容错)
- [数据量估算](#数据量估算)
- [schema 演进](#schema-演进)
- [联调方法](#联调方法)

---

## 上报协议

| 项 | 值 |
|---|---|
| 方法 | `POST <server_url>` |
| Content-Type | `application/json` |
| 鉴权 | KEY 在请求头，头名默认 `X-Monitor-Key`（agent 端可配 `key_header` 改名）；**body 不含 key** |
| 频率 | 每 `interval_minutes` 分钟一条（默认 1 分钟） |
| 单条大小 | 约 1.5–2.5 KB（随挂载点/网卡数量浮动） |
| User-Agent | `ureq/2.12.1` |

## 完整 JSON 示例

```json
{
  "schema_version": 1,
  "agent_version": "0.1.2",
  "ts": 1700000000,
  "label": "web-01",
  "host": {
    "hostname": "web-01",
    "machine_id": "0123456789abcdef0123456789abcdef",
    "product_uuid": "",
    "os": "Ubuntu 22.04.3 LTS",
    "os_id": "ubuntu",
    "os_version": "22.04",
    "kernel": "5.15.0-91-generic",
    "arch": "x86_64",
    "cpu_model": "Intel(R) Xeon(R) Gold 6248 CPU @ 2.50GHz",
    "cpu_cores": 8,
    "uptime_secs": 86400
  },
  "cpu": {
    "usage_percent": 3.21,
    "load_1": 0.12, "load_5": 0.18, "load_15": 0.15,
    "running_procs": 1, "total_procs": 280, "process_count": 152
  },
  "memory": {
    "total_bytes": 16875409408, "used_bytes": 4294967296,
    "available_bytes": 12580442112, "used_percent": 25.45,
    "swap_total_bytes": 2147483648, "swap_used_bytes": 0
  },
  "disks": [
    { "device": "/dev/sda1", "mountpoint": "/", "fstype": "ext4",
      "total_bytes": 53687091200, "used_bytes": 16106127360, "free_bytes": 34896609280, "used_percent": 30.0 }
  ],
  "disk_io": [
    { "device": "sda", "read_bytes_per_sec": 1024.0, "write_bytes_per_sec": 2048.0, "read_iops": 1.0, "write_iops": 2.0 }
  ],
  "networks": [
    { "interface": "eth0", "ipv4": ["10.0.1.20"],
      "ipv6": ["2001:db8:1::a"],
      "rx_bytes": 1048576, "tx_bytes": 524288,
      "rx_bytes_per_sec": 1024.0, "tx_bytes_per_sec": 512.0 }
  ],
  "tcp": { "established": 42, "time_wait": 130, "listen": 8, "total": 240 }
}
```

---

## 字段说明

> **通用约定**
> - 所有**容量**字段单位为 **bytes**（不是 KB/MB）。
> - 所有**百分比**字段为 **0–100 浮点**。
> - 时间戳 `ts` 为 **unix 秒（UTC）**。
> - 可选字段在首周期/采集失败/采集项关闭时**缺省（不出现在 JSON 中）**，而非 `null`。
> - 数组（`disks`/`disk_io`/`networks`）为空时不出现。

### 顶层字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `schema_version` | int | JSON 结构版本号（当前 `1`），见 [schema 演进](#schema-演进) |
| `agent_version` | string | 上报端版本（如 `0.1.2`） |
| `ts` | int | 本条数据生成时刻，unix 秒（UTC） |
| `label` | string | 服务器自定义标识；为空时回退 `hostname` |
| `host` | object | 主机静态元数据 |
| `cpu` | object | CPU 使用率 + 负载 + 进程数 |
| `memory` | object | 内存 |
| `disks` | array | 各挂载点磁盘空间 |
| `disk_io` | array | 各块设备读写速率 |
| `networks` | array | 各网卡流量与 IP |
| `tcp` | object | TCP 连接数统计 |

### `host`

| 字段 | 类型 | 说明 |
|---|---|---|
| `hostname` | string | 主机名 |
| `machine_id` | string | 机器唯一标识（`/etc/machine-id`），跨重启稳定；克隆镜像需重新生成 |
| `product_uuid` | string | DMI 产品 UUID（仅 root 可读，否则空串），作 `machine_id` 重复时的辅助 |
| `os` / `os_id` / `os_version` | string | 操作系统（`PRETTY_NAME` / `ID` / `VERSION_ID`） |
| `kernel` | string | 内核版本 |
| `arch` | string | 架构（`x86_64` / `aarch64`） |
| `cpu_model` | string | CPU 型号 |
| `cpu_cores` | int | 逻辑 CPU 核数 |
| `uptime_secs` | int | 系统已运行秒数 |

### `cpu`

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `usage_percent` | number（可缺省） | % | CPU 使用率（0–100），跨 `/proc/stat` 差值；首周期缺省 |
| `load_1` / `load_5` / `load_15` | number | — | 1/5/15 分钟平均负载 |
| `running_procs` | int | 个 | 运行态任务数 |
| `total_procs` | int | 个 | 总任务/线程数 |
| `process_count` | int | 个 | 进程数 |

### `memory`

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `total_bytes` | int | bytes | 物理内存总量 |
| `used_bytes` | int | bytes | 已用 = `total − available` |
| `available_bytes` | int | bytes | 可用（`MemAvailable`） |
| `used_percent` | number | % | 已用百分比 |
| `swap_total_bytes` | int | bytes | swap 总量 |
| `swap_used_bytes` | int | bytes | swap 已用 |

### `disks[]`（每个真实挂载点一项）

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `device` | string | — | 设备路径（`/dev/sda1` 等） |
| `mountpoint` | string | — | 挂载点 |
| `fstype` | string | — | 文件系统类型 |
| `total_bytes` | int | bytes | 总容量 |
| `used_bytes` | int | bytes | 已用（含 root reserved） |
| `free_bytes` | int | bytes | 普通用户可用 |
| `used_percent` | number | % | 已用百分比 |

### `disk_io[]`（每个顶层块设备一项，已排除分区）

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `device` | string | — | 块设备名（`sda`/`nvme0n1`/`vda`/`dm-0`/`md0` 等） |
| `read_bytes_per_sec` | number | bytes/s | 读吞吐（上一间隔均值） |
| `write_bytes_per_sec` | number | bytes/s | 写吞吐 |
| `read_iops` | number | 次/s | 读完成次数速率 |
| `write_iops` | number | 次/s | 写完成次数速率 |

### `networks[]`（每个非 `lo` 网卡一项）

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `interface` | string | — | 网卡名 |
| `ipv4` | array&lt;string&gt; | — | 全局 IPv4（已过滤链路本地 `169.254/16`），可能为空 |
| `ipv6` | array&lt;string&gt; | — | 全局 IPv6（已过滤链路本地 `fe80::/10`），可能为空 |
| `rx_bytes` | int | bytes | 接收累计字节（单调递增） |
| `tx_bytes` | int | bytes | 发送累计字节 |
| `rx_bytes_per_sec` | number | bytes/s | 接收速率 |
| `tx_bytes_per_sec` | number | bytes/s | 发送速率 |

> 带宽 bps = `bytes_per_sec × 8`。

### `tcp`

| 字段 | 类型 | 单位 | 说明 |
|---|---|---|---|
| `established` | int | 个 | `ESTABLISHED` 连接数 |
| `time_wait` | int | 个 | `TIME_WAIT`（排查堆积） |
| `listen` | int | 个 | `LISTEN` 套接字数 |
| `total` | int | 个 | 全部状态连接数（IPv4+IPv6） |

---

## 主机标识与去重

- 推荐**主键 = `machine_id`**（`/etc/machine-id`，跨重启稳定）。
- `product_uuid` 作辅助：当 `machine_id` 为空或疑似重复时，用 `product_uuid`（DMI，云厂商通常唯一）兜底识别。
- `hostname` / `label` 仅作**展示名**，**不要作主键**（可重名、可改名）。`label` 为空时 agent 自动回退 `hostname`。
- **克隆镜像警告**：批量克隆的虚机 `machine_id` 可能相同 → 会被误判为同一台。建议接收端对"`machine_id` 相同但 `product_uuid`/`hostname` 不同"的情况告警提示（运维侧需在模板清除 machine-id 重新生成）。

## 时间戳与时序处理

- `ts` = agent **本地系统时钟**的 unix 秒（UTC），**依赖部署机 NTP**；时钟不准则 ts 不准。
- 入库应按 `ts`（数据生成时刻），**不要按到达时间**。原因：agent 有**内存补传队列**（默认最多 60 条 ≈ 1 小时），断网期间的数据会在恢复后集中后到，这些数据 `ts` 是历史值。
- 同一主机正常情况 `ts` 单调递增（步长 ≈ `interval_minutes × 60`）；NTP 校时可能导致时钟回拨，建议容忍。
- 按 `主机(machine_id) + ts` 建立时间序列。

## 数据语义

- **差值类（周期内均值，非瞬时）**：`cpu.usage_percent`、`networks[].*_per_sec`、`disk_io[].*_per_sec`、`*_iops`。语义为「上一采集间隔内的平均值」。
- **累计类（开机以来单调递增）**：`networks[].rx_bytes`、`tx_bytes`。可据此校验或重算速率（agent 也已提供 `_per_sec`）。
- **即时快照类**：`memory`、`disks` 空间、`tcp`、`load_*`、`process_count`、`host` 等为采集瞬时的值。
- **数组类**（`disks`/`disk_io`/`networks`）：元素数量**可变**（挂载点/网卡/块设备会增减），按 `device`/`mountpoint`/`interface` 作为时间序列的子 key；元素顺序不保证稳定。

## HTTP 响应约定与重试

agent 只依据 **HTTP 状态码**判定，**不解析响应体**：

| 状态码 | agent 行为 | 接收端含义 |
|---|---|---|
| `2xx` | 成功，清空对应补传队列 | 正常入库；**响应体随意**（可空） |
| `4xx` | 视为「拒绝」，**不重试**，当前数据丢弃 | 用 4xx 表达「这条不要」（如 401 key 非法、400 字段错误） |
| `5xx` / 连接超时 / 网络错误 | 「瞬态错误」，**指数退避重试**（默认最多 3 次，基数 500ms）；耗尽后入补传队列 | 服务端临时故障 |

接收端处理流程：校验 `X-Monitor-Key` → 解析 JSON → 入库 → 返回 `2xx`。

## 幂等与去重

- agent 正常**不会重发已成功（2xx）的数据**。
- 但极端情况（网络抖动致 agent 误判、补传边界）可能出现重复，建议接收端按 **`(machine_id, ts)` 做 upsert 幂等**，重复直接覆盖/忽略。
- 补传数据 `ts` 是历史值（见时序），不是重复，应正常入库。

## 缺省与容错

- 字段缺省（采集项关闭 / 单点失败 / 首周期无基线）时**不出现**，不是 `null`。典型：`cpu.usage_percent` 首次缺省；某挂载点无权限则 `disks` 少一项。
- 数组可能为空或整段不出现；单点失败不阻塞整条上报。
- 建议接收端**忽略 JSON 中未识别的字段**、用默认值容错缺失字段（见 schema 演进）。

## 数据量估算

- 单条 ≈ 1.5–2.5 KB；默认 1 分钟/次 → ≈ **2–4 MB/天/主机**。
- 100 台主机 ≈ **200–400 MB/天**。选型时序库（VictoriaMetrics / InfluxDB / TDengine 等）按此估容量。
- 需降量可调大 agent 的 `interval_minutes`。

## schema 演进

- `schema_version`（当前 `1`）：结构版本号。agent **加字段属向后兼容**（只增不删、不改语义），版本号不变；发生不兼容变更时递增。
- `agent_version`：agent 版本，可用于识别旧版本的行为差异。
- 建议接收端**忽略未识别字段**，以便 agent 平滑升级加字段时不报错。

## 联调方法

1. 用任一装了 agent 的机器产出样例 JSON：
   ```sh
   monitor-agent --once --config /etc/monitor-agent/config.toml > sample.json
   ```
2. 把样例 POST 到你的接收端，验证解析与入库：
   ```sh
   curl -X POST http://你的接收端/data/server \
     -H "X-Monitor-Key: test" \
     -H "Content-Type: application/json" \
     -d @sample.json
   ```
3. 或临时把一台 agent 的 `server.url` 指向你的接收端进行端到端联调。
